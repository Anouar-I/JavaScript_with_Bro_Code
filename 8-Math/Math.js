// Math = built-in object that provides a 
//         math related collection of properties and methods

        /*
        Math.PI

        console.log(Math.PI);
        console.log(Math.E);
        */

let x = -81;
let y = 2;
let z = 3;
// round = rounds numb  / / floor = rounds down / / ceil = rounds up / / trunc = trunckated  remvoes decimal   
//                          pow = powwer of     / / sqrt = square-root
        /*
        z = Math.round(x);
        z = Math.floor(x);
        z = Math.ceil(x);
        z = Math.trunc(x);
        z = Math.pow(x);
        z = Math.sqrt(x);
        z = Math.log(x);
        z = Math.sin(x);
        z = Math.cos(x);
        z = Math.tan(x);
        z = Math.abs(x); absolute if numb was negatice gives postive
        z = Math.sign(x);
        let max = Math.max(x,y,z) // display the max value 
        */
       
         let min = Math.min(x,y,z) // display the min value 


       

console.log(min);